# EDA-report-test

Исходный файл: `test_custom_heuristics.csv`

Строк: **200**, столбцов: **5**

## Качество данных (эвристики)

- Оценка качества: **0.55**
- Макс. доля пропусков по колонке: **35.00%**
- Макс. доля нулевых значений по колонке: **75.00%**
- Слишком мало строк: **False**
- Слишком много колонок: **False**
- Слишком много пропусков: **False**
- Слишком много нулевых значений: **True**
- Дублирующиеся id-колонки: **True**
- Слишком много уникальных значений в категориальных признаках: **True**

## Колонки

Подробный отчет - см. файл `summary.csv`.

## Пропуски

Проблемные колонки (не удовлетворяют порогу min_missing_share):
missing_column
См. файлы `missing.csv` и `missing_matrix.png`.

## Корреляция числовых признаков

См. `correlation.csv` и `correlation_heatmap.png`.

## Категориальные признаки

Количество top-значений для категориальных признаков: 3
См. файлы в папке `top_categories/`.

## Гистограммы числовых колонок

См. файлы `hist_*.png`.
