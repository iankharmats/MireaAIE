# S03 – eda_cli: мини-EDA для CSV

Небольшое CLI-приложение для базового анализа CSV-файлов.
Используется в рамках Семинара 03 курса «Инженерия ИИ».

## Требования

- Python 3.11+
- [uv](https://docs.astral.sh/uv/) установлен в систему

## Инициализация проекта

В корне проекта (S03):

```bash
uv sync
```

Эта команда:

- создаст виртуальное окружение `.venv`;
- установит зависимости из `pyproject.toml`;
- установит сам проект `eda-cli` в окружение.

## Запуск CLI

### Краткий обзор

```bash
uv run eda-cli overview data/example.csv
```

Параметры:

- `--sep` – разделитель (по умолчанию `,`);
- `--encoding` – кодировка (по умолчанию `utf-8`).

### Вывод случайных строк
```bash
uv run eda-cli sample data/example.csv
```

Параметры:

- `--n` – количество строк (по умолчанию `3`);
- `--sep` – разделитель (по умолчанию `,`);
- `--encoding` – кодировка (по умолчанию `utf-8`).

### Полный EDA-отчёт

```bash
uv run eda-cli report data/example.csv --out-dir reports
```

Параметры:

- `--sep` – разделитель (по умолчанию `,`);
- `--encoding` – кодировка (по умолчанию `utf-8`);
- `--out-dir` – каталог для отчёта. (по умолчанию `reports`);
- `--max-hist-columns` – максимум числовых колонок для гистограмм. (по умолчанию `6`);
- `--top-k-categories` – количество top-значений для категориальных признаков. (по умолчанию `5`);
- `--min-missing-share` – порог доли пропусков, выше которого колонка считается проблемной. (по умолчанию `0.5`);
- `--title` – заголовок отчёта (по умолчанию `EDA-отчёт`);
- `--json-summary` – флаг генерации краткого отчета summary.json (по умолчанию `False`).

Пример полного вызова:
```bash
uv run eda-cli report data/test_custom_heuristics.csv --out-dir reports_example --sep , --encoding utf-8 --max-hist-columns 5 --top-k-categories 3 --min-missing-share 0.3 --title EDA-report-test --json-summary
```

В результате в каталоге `reports/` появятся:

- `report.md` – основной отчёт в Markdown;
- `summary.csv` – таблица по колонкам;
- `missing.csv` – пропуски по колонкам;
- `correlation.csv` – корреляционная матрица (если есть числовые признаки);
- `top_categories/*.csv` – top-k категорий по строковым признакам;
- `hist_*.png` – гистограммы числовых колонок;
- `missing_matrix.png` – визуализация пропусков;
- `correlation_heatmap.png` – тепловая карта корреляций.

## Запуск Web-приложения
```bash
uv run uvicorn eda_cli.api:app --reload --port 8000
```
Команда запускает FastAPI приложение app из файла eda_cli/api.py на порту 8000 с автоперезагрузкой.

## API - запросы

### SYSTEM
- `GET /health` - health-check сервиса.
- `GET /metrics` - статистика по работе сервиса: общее количество реквест-запросов, среднее время выполнения запросов, информация об обработке последнего качественного датасета 
### QUALITY
- `POST /quality` - оценка качества датасета по агрегированным признакам. Параметры: `dict[str, int]` - словарь признаков (количество строк, столбцов, макс. доля пропусков, количество числовых и категориальных столбцов в соответствии с примером).
- `POST /quality-from-csv` - подробная оценка качества данных CSV. Параметры: `file` - исходный CSV-файл.
- `POST /quality-flags-from-csv` - полный набор флагов качества CSV. Параметры: `file` - исходный CSV-файл.
- `POST /summary-from-csv` - краткая json-сводка по CSV. Параметры: `file` - исходный CSV-файл.
### EXPLORATION
- `POST /sample` - вывод n случайных строк CSV. Параметры: `file` - исходный CSV-файл; `n` - количество возвращаемых строк.

### Структурированное логирование запросов
```bash
logs/api.log 
```

## Тесты

```bash
uv run pytest -q
```
